name: Deploy E-Ticketing App

on:
  push:
    branches: ["main"]
    paths:
      - 'backend/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Deploy via SSH
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.VPS_HOST }}
        username: ${{ secrets.VPS_USERNAME }}
        key: ${{ secrets.VPS_KEY }}
        port: ${{ secrets.VPS_PORT }}
        script: |
          cd /home/root/e-ticketing 

          echo "Pulling latest code..."
          git pull origin main

          cd backend
          echo "Cleaning up old containers and images..."
          podman-compose down --remove-orphans || true

          podman rm -f e-ticketing-app e-ticketing-worker e-ticketing-scheduler e-ticketing-redis e-ticketing-postgres 2>/dev/null || true

          echo "Building and Restarting Services..."
          podman-compose up -d --build --remove-orphans

          echo "Running Database Migrations..."
          podman exec e-ticketing-app php artisan migrate --force

          echo "Cleaning up old images..."
          podman image prune -af

          echo "Deployment Success"
          podman ps